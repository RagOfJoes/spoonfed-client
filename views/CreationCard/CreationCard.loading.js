import Skeleton from '@material-ui/lab/Skeleton';

const CreationCardLoading = ({}) => {
	return <Skeleton height={390} width="100%" variant="rect" style={{ minWidth: 280, maxWidth: 640 }} />;
};

export default CreationCardLoading;
